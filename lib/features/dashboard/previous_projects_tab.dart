import 'package:flutter/material.dart';

class PreviousProjectsTab extends StatelessWidget {
  const PreviousProjectsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Previous Projects Tab Placeholder'));
  }
}
